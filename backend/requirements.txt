fastapi==0.115.6
uvicorn[standard]==0.34.0
sse-starlette==2.1.3
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

langgraph==1.0.1
langchain==0.3.30
langchain-core==0.3.86
langchain-groq==0.2.2
deepagents==0.0.3
tavily-python==0.5.0

# Persistent checkpointer (Supabase / any Postgres). Optional at runtime —
# falls back to MemorySaver when DATABASE_URL is unset.
langgraph-checkpoint-postgres==2.0.25
psycopg[binary,pool]==3.3.4

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
ruff==0.8.4
